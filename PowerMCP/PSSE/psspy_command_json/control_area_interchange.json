{
  "module": "psspy",
  "function_name": "control_area_interchange",
  "category": "Power Flow Operation",
  "description": "Use this API to to specify or return the area interchange control option setting to disabled, or enabled with tie lines only, or enabled with tie lines and loads. This API routine was first introduced in release 30.0.0. ierr = control_area_interchange(ival) ierr, ival = control_area_interchange()",
  "python_syntax": "ierr = control_area_interchange(ival) ierr, ival = control_area_interchange()",
  "return_type": "error_only",
  "parameters": [
    {
      "name": "ival",
      "description": "Value of the option setting. (input; if Batch command, or for Python if specified as an argument, or for Fortran if IOCODE is 0, then input of 0 by default; else output)."
    }
  ],
  "return_values": [
    {
      "name": "ierr",
      "description": "Error code. (output)."
    }
  ],
  "allowed_values": {
    "ival": [
      {
        "value": "= 0",
        "description": "disabled."
      },
      {
        "value": "= 1",
        "description": "enabled with interchange determined from tie lines only."
      },
      {
        "value": "= 2",
        "description": "enabled with interchange determined from tie lines and loads."
      }
    ]
  },
  "error_codes": [
    {
      "value": "= 0",
      "description": "no error occurred."
    },
    {
      "value": "= 1",
      "description": "invalid IVAL value."
    },
    {
      "value": "= 2",
      "description": "invalid IOCODE value."
    },
    {
      "value": "= 3",
      "description": "prerequisite requirements for API are not met."
    }
  ]
}