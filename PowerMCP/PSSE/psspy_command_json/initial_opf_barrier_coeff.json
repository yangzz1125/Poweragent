{
  "module": "psspy",
  "function_name": "initial_opf_barrier_coeff",
  "category": "Optimal Power Flow Operation",
  "description": "Use this API to specify or return the value of the initial OPF barrier coefficient. It may be used in conjunction with the FINAL_OPF_BARRIER_COEFF value. This API routine was first introduced in release 30.0.0. ierr = initial_opf_barrier_coeff(rval) ierr, rval = initial_opf_barrier_coeff()",
  "python_syntax": "ierr = initial_opf_barrier_coeff(rval) ierr, rval = initial_opf_barrier_coeff()",
  "return_type": "error_only",
  "parameters": [
    {
      "name": "rval",
      "description": "Initial barrier coefficient value. (input; if Batch command, or for Python if specified as an argument, or for Fortran if IOCODE is 0, then input of 0 by default; else output)."
    }
  ],
  "return_values": [
    {
      "name": "ierr",
      "description": "Error code. (output)."
    }
  ],
  "error_codes": [
    {
      "value": "= 0",
      "description": "no error."
    },
    {
      "value": "= 1",
      "description": "invalid RVAL value."
    },
    {
      "value": "= 2",
      "description": "invalid IOCODE value."
    },
    {
      "value": "= 3",
      "description": "prerequisite requirements for API are not met."
    }
  ]
}