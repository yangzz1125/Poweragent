{
  "module": "psspy",
  "function_name": "interface_flow_cost_coeff",
  "category": "Optimal Power Flow Operation",
  "description": "Use this API to specify or return the value of the coefficient for the interface flow objective. This API works in conjunction with the MINIMIZE_INTERFACE_FLOWS objective function option. This API routine was first introduced in release 30.0.0. ierr = interface_flow_cost_coeff(rval) ierr, rval = interface_flow_cost_coeff()",
  "python_syntax": "ierr = interface_flow_cost_coeff(rval) ierr, rval = interface_flow_cost_coeff()",
  "return_type": "error_only",
  "parameters": [
    {
      "name": "rval",
      "description": "Value of the interface flow cost coefficient. (input; if Batch command, or for Python if specified as an argument, or for Fortran if IOCODE is 0, then input of 0 by default; else output)."
    }
  ],
  "return_values": [
    {
      "name": "ierr",
      "description": "Error code. (output)."
    }
  ],
  "error_codes": [
    {
      "value": "= 0",
      "description": "no error."
    },
    {
      "value": "= 1",
      "description": "invalid RVAL value."
    },
    {
      "value": "= 2",
      "description": "invalid IOCODE value."
    },
    {
      "value": "= 3",
      "description": "prerequisite requirements for API are not met."
    }
  ]
}