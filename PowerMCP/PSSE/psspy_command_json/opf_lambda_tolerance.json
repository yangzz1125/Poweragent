{
  "module": "psspy",
  "function_name": "opf_lambda_tolerance",
  "category": "Optimal Power Flow Operation",
  "description": "Use this API to specify or return the value of the Lagrange multiplier blow-up tolerance during an optimal power flow solution. This API routine was first introduced in release 30.0.0. ierr = opf_lambda_tolerance(rval) ierr, rval = opf_lambda_tolerance()",
  "python_syntax": "ierr = opf_lambda_tolerance(rval) ierr, rval = opf_lambda_tolerance()",
  "return_type": "error_only",
  "parameters": [
    {
      "name": "rval",
      "description": "Lambda tolerance value. (input; if Batch command, or for Python if specified as an argument, or for Fortran if IOCODE is 0, then input of 0 by default; else output)."
    }
  ],
  "return_values": [
    {
      "name": "ierr",
      "description": "Error code. (output)."
    }
  ],
  "error_codes": [
    {
      "value": "= 0",
      "description": "no error."
    },
    {
      "value": "= 1",
      "description": "invalid RVAL value."
    },
    {
      "value": "= 2",
      "description": "invalid IOCODE value."
    },
    {
      "value": "= 3",
      "description": "prerequisite requirements for API are not met."
    }
  ]
}