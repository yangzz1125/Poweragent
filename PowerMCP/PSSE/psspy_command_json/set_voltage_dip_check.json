{
  "module": "psspy",
  "function_name": "set_voltage_dip_check",
  "category": "Dynamic Simulation Operation",
  "description": "This API is used to enable or disable the simulation option setting which scans buses for voltage dip (back-swing). This API routine was first introduced in release 32.0.0. ierr = set_voltage_dip_check(vdpchk, vdpthr, vdpdur) ierr, vdpchk, vdpthr, vdpdur = set_voltage_dip_check()",
  "python_syntax": "ierr = set_voltage_dip_check(vdpchk, vdpthr, vdpdur) ierr, vdpchk, vdpthr, vdpdur = set_voltage_dip_check()",
  "return_type": "error_only",
  "parameters": [
    {
      "name": "vdpchk",
      "description": "None. (input; flag to set voltage dip check: if command or ICODE is 0 or negative; unchanged by default)."
    },
    {
      "name": "vdpthr",
      "description": "None. (input; voltage dip threshold in per unit: if batch command or ICODE is 0 or negative; unchanged by default. if ICODE is positive. If the voltage swings-back below this threshold a timer is imitated to check for the dip duration)."
    },
    {
      "name": "vdpdur",
      "description": "None. (input; voltage dip duration in seconds: if batch command or ICODE is 0 or negative; unchanged by default)."
    }
  ],
  "return_values": [
    {
      "name": "ierr",
      "description": "Error code. (output)."
    }
  ],
  "allowed_values": {
    "vdpchk": [
      {
        "value": "= 0",
        "description": "disable voltage dip check."
      },
      {
        "value": "= 1",
        "description": "enable voltage dip check."
      }
    ]
  },
  "error_codes": [
    {
      "value": "= 0",
      "description": "no error."
    },
    {
      "value": "= 1",
      "description": "prerequisite requirements for API not met."
    }
  ]
}