{
  "module": "psspy",
  "function_name": "ties",
  "category": "Power Flow Operation",
  "description": "Use this API to tabulate the flows on all area tie lines, with tie flows grouped by area. This API routine was first introduced in release 28.0.0. ierr = ties(sid, all)",
  "python_syntax": "ierr = ties(sid, all)",
  "return_type": "error_only_report",
  "parameters": [
    {
      "name": "sid",
      "description": "A valid area subsystem identifier. valid subsystem identifiers range from 0 to 11. area subsystem sid must have been previously defined. (input; 0 by default)."
    },
    {
      "name": "all",
      "description": "All areas or specified subsystem flag. (input; 1 by default)."
    }
  ],
  "return_values": [
    {
      "name": "ierr",
      "description": "Error code. (output)."
    }
  ],
  "allowed_values": {
    "all": [
      {
        "value": "= 0",
        "description": "process only areas in area subsystem SID."
      },
      {
        "value": "= 1",
        "description": "process all areas."
      }
    ]
  },
  "error_codes": [
    {
      "value": "= 0",
      "description": "no error occurred."
    },
    {
      "value": "= 1",
      "description": "invalid SID value or subsystem SID is not defined."
    },
    {
      "value": "= 2",
      "description": "invalid ALL value."
    },
    {
      "value": "= 3",
      "description": "prerequisite requirements for API are not met."
    }
  ]
}